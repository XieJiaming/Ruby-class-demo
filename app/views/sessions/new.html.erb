<h1>會員登入</h1>

<%= form_with(model: @user, url: login_path, method: 'post', local:true) do |f| %>
    <%= f.label :email %>
    <%= f.text_field :email %>
    <%= f.label :password %>
    <%= f.password_field :password %>

    <%= f.submit '登入' %>
<% end %>
